#include <stdio.h>

/**
 * program that prints string using the puts() function
 *
 * Return: Always 0 (Success)
 */

int main()
{
	char myStr[] = "Programming is like building a multilingual puzzle";
	puts(myStr);
	return (0);
}
